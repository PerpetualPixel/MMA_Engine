# Secrets — never commit these
.env
.env.*
!.env.example
# Exported YouTube cookies for the age-restricted transcript fallback — a live
# session token, every bit as sensitive as an API key.
cookies.txt

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
test_env/
.pytest_cache/

# Pipeline artifacts
cache/
*.log

# OS / editor
.DS_Store
.idea/
.vscode/
