youtube-transcript-api>=1.2,<2
# Authenticated fallback for age-restricted / PoToken-gated videos the
# anonymous transcript API can't read (see mma_engine.transcripts). Invoked as
# `python -m yt_dlp` through the running interpreter, so it just needs to be
# importable in the same venv — no PATH/console-script dependency.
yt-dlp>=2024.1
requests>=2.31
anthropic>=0.121,<1
python-dotenv>=1.0
pydantic>=2.7,<3
